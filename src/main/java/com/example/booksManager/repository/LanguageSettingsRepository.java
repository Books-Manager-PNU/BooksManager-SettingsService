package com.example.booksManager.repository;

import com.example.booksManager.entity.LanguageSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LanguageSettingsRepository extends JpaRepository<LanguageSettings, Long> {
}
